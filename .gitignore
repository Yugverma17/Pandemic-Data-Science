# Data — regenerated by `python -m scripts.fetch_data`, never committed
data/raw/
data/interim/
*.parquet
*.csv.gz

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
build/
dist/

# Reference PDFs kept alongside the project locally. Excluded to keep the repo
# small: they run to ~58 MB and none of the pipeline reads them.
*.pdf

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Keep the committed outputs: reports/figures/*.png and reports/tables/*.csv are
# deliberately tracked so the README renders on GitHub without running anything.
